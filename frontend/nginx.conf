server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        # THIS IS THE MAGIC LINE: It routes all traffic to React!
        try_files $uri $uri/ /index.html;
    }
}